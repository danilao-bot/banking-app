{
  "name": "bank-system",
  "version": "1.0.0",
  "description": "Full-stack Core Banking & Aether Retail Wallet Application",
  "scripts": {
    "install:all": "npm install --prefix frontend && cd backend && python -m pip install -r requirements.txt",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && uvicorn app:app --reload --port 8000",
    "dev": "npx concurrently -n \"frontend,backend\" -c \"cyan,magenta\" \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
